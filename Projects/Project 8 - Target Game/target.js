class Target {
    constructor(x,y) {
        this.x = int(random(100, width));
        this.y = int(random(120, 546));


    }
        

    display() {
        image(targetImage, this.x, this.y);
    }
    









}